# filter.yaml – optional allow-list of games to display.
# Only the games named here will appear in the launcher.
# Set sync_from_github to true to automatically update this list from the GitHub repository or set it to false to manage the list manually.
# Set enabled to true to enable the filter or false to disable it and show all games.

enabled: true
sync_from_github: true
games:
- Battlefield 4